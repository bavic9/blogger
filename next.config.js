/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        // domains:["https://lh3.googleusercontent.com","firebasestorage.googleapis.com"]
        domains: ['lh3.googleusercontent.com'],
      }
}

module.exports = nextConfig
